---
phase: 03-latent-circuit-inference
plan: "08"
type: execute
wave: 7
depends_on: ["03-05", "03-06", "03-07"]
gap_closure: true
autonomous: false
files_modified:
  - output/circuit_analysis/wave_b_writeup.md
  - output/circuit_analysis/phase_3_1_evidence.json
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/phases/03-latent-circuit-inference/03-08-SUMMARY.md

must_haves:
  truths:
    - "A consolidated `output/circuit_analysis/phase_3_1_evidence.json` exists, aggregating per-plan corr metrics from 03-05 (masked), 03-06 (per-context), and 03-07 (short-T or skipped) with a single `final_best_corr` and `final_chosen_q_path` field"
    - "wave_b_writeup.md is updated (or replaced; either is acceptable) to reflect the Phase 3.1 evidence chain — Story commitment is one of: STORY_0 (passed, any gap cleared 0.85), STORY_1 (method/data limit, all deferred fixes exhausted), STORY_2 unchanged (only if Phase 3.1 was somehow inconclusive — should not happen)"
    - "User has reviewed the story re-commit at a checkpoint and approved before Phase 4 is officially unblocked"
    - "REQUIREMENTS.md CIRC-04 status reflects final outcome (PASSED or DOCUMENTED_LIMIT) with provenance"
    - "ROADMAP.md Phase 3.1 entry shows all four gap-closure plans (03-05..03-08) with checkboxes; Phase 4 dependency note clarifies that Phase 4 may proceed regardless of Story 0/1 outcome"
  artifacts:
    - path: "output/circuit_analysis/phase_3_1_evidence.json"
      provides: "Single canonical aggregation of all Phase 3.1 evidence: per-plan corrs, per-context conclusion, short-T or skipped, final story, final_best_corr, final_chosen_q_path"
      contains: "final_best_corr"
    - path: "output/circuit_analysis/wave_b_writeup.md"
      provides: "Updated Phase 3 scientific commitment incorporating Phase 3.1 evidence"
      contains: "Phase 3.1"
    - path: ".planning/REQUIREMENTS.md"
      provides: "CIRC-04 status updated to PASSED (if any gap cleared 0.85) or DOCUMENTED_LIMIT (if exhausted)"
      contains: "CIRC-04"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 3.1 plans listed with checkboxes, Phase 4 dependency note updated"
      contains: "03-05"
  key_links:
    - from: "03-08 task 1"
      to: "03-05 wave_a_masked_selection.json + 03-06 per_context_results.json + 03-07 short_T_results.json (or skipped marker)"
      via: "JSON parse + aggregation; missing files → fail loudly with expected vs actual paths"
      pattern: "phase_3_1_evidence"
    - from: "03-08 task 2 (checkpoint)"
      to: "wave_b_writeup.md story commitment"
      via: "User reviews proposed updated writeup BEFORE commit; checkpoint blocks Phase 4 unblock"
      pattern: "Story commitment"
---

<objective>
Phase 3.1 closure plan. Aggregate evidence from Plans 03-05 (masked-loss fit), 03-06 (per-context fit), and 03-07 (short-T fit, possibly skipped) into a single canonical record. Update `wave_b_writeup.md` to reflect the new Phase 3.1 evidence chain. Re-commit Phase 3's scientific story (Story 0 / Story 1 / Story 2) based on the combined evidence. User approves the story re-commit at a checkpoint before Phase 4 is officially unblocked.

Decision rule for the story re-commit:
- STORY_0 (passed): max final corr across {Wave A pooled 0.7833, Wave A masked from 03-05, per-context max from 03-06, short-T from 03-07} ≥ 0.85.
- STORY_1 (method/data limit, all deferred fixes exhausted): max < 0.85 AND all three gap-closure plans executed (03-07 may be SKIPPED only if 03-05 already cleared 0.85, in which case Story 0 anyway).
- STORY_2 (ran out of fixes, unchanged from 03-04): should NOT occur — if reached, planning has a logic error and the user must adjudicate.

Output: phase_3_1_evidence.json; updated wave_b_writeup.md; updated REQUIREMENTS.md (CIRC-04); updated ROADMAP.md (Phase 3.1 plans + Phase 4 dependency); 03-08-SUMMARY.md. Phase 4 unblocked.
</objective>

<execution_context>
@C:\Users\aman0087\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\aman0087\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-latent-circuit-inference/03-CONTEXT.md
@.planning/phases/03-latent-circuit-inference/03-VERIFICATION.md
@.planning/phases/03-latent-circuit-inference/03-04-SUMMARY.md
@.planning/phases/03-latent-circuit-inference/03-05-SUMMARY.md
@.planning/phases/03-latent-circuit-inference/03-06-SUMMARY.md
@.planning/phases/03-latent-circuit-inference/03-07-SUMMARY.md
@output/circuit_analysis/wave_b_writeup.md
@output/circuit_analysis/n_latent_sweep/wave_a_selection.json
@output/circuit_analysis/n_latent_sweep_masked/wave_a_masked_selection.json
@output/circuit_analysis/per_context/per_context_results.json
@output/circuit_analysis/short_T/short_T_results.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Aggregate Phase 3.1 evidence into phase_3_1_evidence.json + draft updated wave_b_writeup.md</name>
  <files>
    output/circuit_analysis/phase_3_1_evidence.json
    output/circuit_analysis/wave_b_writeup.md
  </files>
  <action>
    A) Verify all upstream artifacts exist:
       - `output/circuit_analysis/n_latent_sweep_masked/wave_a_masked_selection.json` (from 03-05) — REQUIRED.
       - `output/circuit_analysis/per_context/per_context_results.json` (from 03-06) — REQUIRED.
       - `output/circuit_analysis/short_T/short_T_results.json` (from 03-07) — REQUIRED if 03-07 not skipped, else `.planning/phases/03-latent-circuit-inference/03-07-SUMMARY.md` must show `status: skipped`.
       Fail with expected vs actual paths if any missing.

    B) Build `output/circuit_analysis/phase_3_1_evidence.json`:
       ```
       {
         "wave_a_pooled": {
           "n_latent": 12,
           "corr": 0.7833,
           "source": "output/circuit_analysis/n_latent_sweep/wave_a_selection.json",
           "q_path": "output/circuit_analysis/best_latent_circuit_waveA.pt"
         },
         "gap_1_masked": {
           "chosen_rank": <int>,
           "corr": <float>,
           "delta_vs_wave_a": <float>,
           "crossed_85_threshold": <bool>,
           "source": "output/circuit_analysis/n_latent_sweep_masked/wave_a_masked_selection.json",
           "q_path": "output/circuit_analysis/n_latent_sweep_masked/n{chosen_rank}/best_latent_circuit.pt"
         },
         "gap_3_per_context": {
           "context_0_corr": <float>,
           "context_1_corr": <float>,
           "max_per_context_corr": <float>,
           "conclusion": "<STRUCTURAL_SEPARATION|NO_SEPARATION|AMBIGUOUS>",
           "either_above_85": <bool>,
           "source": "output/circuit_analysis/per_context/per_context_results.json"
         },
         "gap_2_short_T": {
           "status": "<executed|skipped>",
           "T": <int|null>,
           "corr": <float|null>,
           "crossed_85_threshold": <bool|null>,
           "rnn_degradation_warning": <bool|null>,
           "source": "output/circuit_analysis/short_T/short_T_results.json"
         },
         "summary": {
           "all_corrs": [0.7833, <gap1>, <gap3_max>, <gap2 or null>],
           "final_best_corr": <max of the above non-null>,
           "final_best_source": "<wave_a|gap_1_masked|gap_3_per_context|gap_2_short_T>",
           "final_chosen_q_path": "<path to the .pt corresponding to final_best_source>",
           "story_commitment": "<STORY_0|STORY_1|STORY_2>",
           "phase_4_unblocked": true,
           "eval_procedure_consistent": "cluster_same_seed_as_train across all gap-closure runs"
         }
       }
       ```

       CRITICAL aggregation rule for `final_best_corr`: when 03-07 is SKIPPED (because 03-05 already cleared 0.85 → STORY_0), `gap2_corr` is `None`. Python's built-in `max([..., None, ...])` raises TypeError. The success path is the path most likely to crash. Compute `final_best_corr` as:

       ```python
       all_corrs = [0.7833, gap1_corr, gap3_max, gap2_corr]  # gap2_corr may be None
       assert any(c is not None for c in all_corrs), (
           "all_corrs must have at least one non-None value (Wave A pooled corr 0.7833 is always present)"
       )
       final_best_corr = max(c for c in all_corrs if c is not None)
       ```

       The same generator-with-filter pattern must be used for any other reduction across `all_corrs` in this aggregator (e.g., `argmax` to derive `final_best_source`). Never pass a list containing `None` to a built-in min/max.

       Apply the decision rule:
       - If `final_best_corr >= 0.85` → `story_commitment = "STORY_0"`.
       - Else if all three gap-closure plans completed (03-07 either executed or correctly-skipped because 03-05 succeeded; 03-06 ran independently) → `story_commitment = "STORY_1"`.
       - Else → `story_commitment = "STORY_2"` and FLAG with a `WARNING: planning logic error — should not occur` field; halt and surface to user before proceeding.

       Cast all numpy scalars to Python int/float/bool/str before json.dump.

    C) Draft updated `output/circuit_analysis/wave_b_writeup.md` (in-place edit, NOT a new file). Sections to add/modify:
       - Update line 6 `**Story commitment:**` to the new value (STORY_0 or STORY_1).
       - Add a new top-level section `## Phase 3.1 Evidence` after the existing `## Evidence Summary` block:
         * Subsection: "Gap 1 — Masked-loss fitting (03-05)": per-rank table from masked sweep, chosen rank, delta vs Wave A, crossed_85_threshold.
         * Subsection: "Gap 3 — Per-context fits (03-06)": per-context corr, conclusion enum, interpretation paragraph.
         * Subsection: "Gap 2 — Shorter-T regen (03-07)": status (executed or skipped), corr if executed, RNN-degradation note.
       - Replace the `## Commitment` paragraph wholesale with the new commitment matching `story_commitment`. If STORY_0, the new commitment paragraph cites which gap cleared 0.85, references the chosen Q path, and notes Phase 4 proceeds with a strengthened (not weakened) latent circuit foundation. If STORY_1, the paragraph cites that all three deferred fixes were exhausted without crossing 0.85 and reframes the conclusion from "ran out of fixes" (Story 2) to "method/data limit at this hidden_dim/task combo" (Story 1) — explicitly noting this is the strongest scientific commitment compatible with the evidence.
       - Update the `**Deferred fixes**` section: change from a list of "NOT included" candidates (current state — line 73) to a list of "Implemented in Phase 3.1" with cross-references to 03-05/06/07 SUMMARYs and metric outcomes.
       - Append a `## Phase 3.1 Closure` section: state that Phase 3.1 is complete, all SC-2 deferred fixes attempted, final corr value reported, story commitment final.
       - The `## Files` section gets new entries for phase_3_1_evidence.json, n_latent_sweep_masked/, per_context/, short_T/.

       DO NOT delete the existing Wave A or Wave B evidence sections — they remain the chronological record.

       Conventions: Use markdown only (no emojis per project conventions).
  </action>
  <verify>
    `python -c "import json; d = json.load(open('output/circuit_analysis/phase_3_1_evidence.json')); assert d['summary']['story_commitment'] in ('STORY_0', 'STORY_1', 'STORY_2'); assert isinstance(d['summary']['final_best_corr'], float); assert d['summary']['phase_4_unblocked'] is True; print(d['summary']['story_commitment'], d['summary']['final_best_corr'])"`

    `grep -c "^## Phase 3.1 Evidence" output/circuit_analysis/wave_b_writeup.md` returns 1.
    `grep -c "^## Phase 3.1 Closure" output/circuit_analysis/wave_b_writeup.md` returns 1.
    `grep "^\\*\\*Story commitment:\\*\\*" output/circuit_analysis/wave_b_writeup.md` shows the NEW story value (STORY_0 or STORY_1).

    The pre-update wave_b_writeup.md (Story 2) is preserved in git history.
  </verify>
  <done>
    - phase_3_1_evidence.json exists with valid JSON, correct story_commitment per the decision rule, all numbers are Python types.
    - wave_b_writeup.md updated in-place with Phase 3.1 Evidence + Phase 3.1 Closure sections; story commitment line reflects new value.
    - No new files created besides phase_3_1_evidence.json and the SUMMARY (Task 3).
    - Decision logic correctly handles SKIPPED 03-07 (counts as "completed via 03-05 success" for the all-completed check).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Phase 3.1 evidence aggregator + draft updated wave_b_writeup.md committing to a new story (STORY_0 or STORY_1) based on combined evidence from Plans 03-05, 03-06, 03-07.
  </what-built>
  <how-to-verify>
    1. Read `output/circuit_analysis/phase_3_1_evidence.json`. Confirm `summary.final_best_corr`, `summary.final_best_source`, `summary.story_commitment`, and `summary.final_chosen_q_path` match your read of the underlying per-plan results.
    2. Read the updated `output/circuit_analysis/wave_b_writeup.md`. Verify:
       - Line 6 `**Story commitment:**` value is correct.
       - Phase 3.1 Evidence section has tables/numbers matching the per-plan SUMMARYs.
       - Phase 3.1 Closure section accurately describes the outcome.
       - Commitment paragraph (replacing the old Story 2 commitment) reads scientifically defensible.
    3. Confirm: do you want this story to become Phase 3's final commitment? (Editorial review opportunity: tone, scientific defensibility, narrative.)
    4. Confirm: is Phase 4 ready to proceed?
  </how-to-verify>
  <resume-signal>
    Type "approved" to proceed with REQUIREMENTS.md / ROADMAP.md updates and the SUMMARY commit.
    Type "edit: <changes>" to revise the writeup before approval.
    Type "story-disagree: <reason>" if you read the evidence differently and want a different story (rare — would require revising the decision rule itself).
  </resume-signal>
</task>

<task type="auto">
  <name>Task 3: Update REQUIREMENTS.md (CIRC-04) + ROADMAP.md (Phase 3.1 plans) + 03-08-SUMMARY.md + commit Phase 3.1 closure</name>
  <files>
    .planning/REQUIREMENTS.md
    .planning/ROADMAP.md
    .planning/phases/03-latent-circuit-inference/03-08-SUMMARY.md
  </files>
  <action>
    Only execute after the Task 2 checkpoint is approved.

    A) Update `.planning/REQUIREMENTS.md`:
       - Locate the CIRC-04 entry (invariant subspace requirement).
       - If story_commitment == STORY_0: mark CIRC-04 as PASSED; record the final corr value and the chosen Q path.
       - If story_commitment == STORY_1: mark CIRC-04 as DOCUMENTED_LIMIT (or whatever the project's existing terminology is — read the file before editing); record the max corr achieved across all gap-closure attempts and a one-line reason ("All three Phase 3.1 deferred fixes attempted; max corr below 0.85 threshold; method/data limit at this hidden_dim/task combo").
       - Reference phase_3_1_evidence.json as the canonical evidence record.
       - CIRC-05 (perturbation analysis) status remains as set by 03-04; do not touch unless story_commitment == STORY_0 AND the user wants Wave B re-run with the new Q (NOT in scope for this plan — would be a separate Phase 3.1.1).

    B) Update `.planning/ROADMAP.md`:
       - In the Phase 3 section, add a new sub-block "Phase 3.1 (gap closure)":
         ```
         Plans:
         - [x] 03-05-PLAN.md — Gap 1: masked-loss fitting (Wave 5)
         - [x] 03-06-PLAN.md — Gap 3: condition-sliced fitting (Wave 5)
         - [x] 03-07-PLAN.md — Gap 2: shorter T regen (Wave 6, may be SKIPPED)
         - [x] 03-08-PLAN.md — Phase 3.1 closure: writeup + story re-commit (Wave 7)
         ```
         Tick all four boxes only after this plan completes.
       - Update the Phase 3 success criteria block to reflect the new SC-2 status (PASSED or DOCUMENTED_LIMIT).
       - Phase 4 dependency note: clarify "Phase 4 proceeds regardless of Story 0/1 outcome — Q quality is descriptive, not gating, for Bayesian fitting" (this was already the position from 03-04; just make it explicit at the roadmap level).

    C) Update `.planning/STATE.md`:
       - Update "Current focus" to reflect Phase 3.1 completion.
       - Move Phase 3.1 blockers from "OPEN" to "RESOLVED" with the resolution outcome (STORY_0 or STORY_1).
       - Update the Phase 3 progress entry: "4/4 base plans + 4/4 Phase 3.1 plans complete".
       - Set "Last activity" to today's date with the Phase 3.1 closure commit reference.

    D) Write `.planning/phases/03-latent-circuit-inference/03-08-SUMMARY.md`:
       - Frontmatter (`phase`, `plan: "08"`, `subsystem: planning-closure`, `gap_closure: true`, `requires: [03-05, 03-06, 03-07]`, `provides: [phase_3_1_evidence.json, updated wave_b_writeup.md, REQUIREMENTS.md CIRC-04 status, ROADMAP.md Phase 3.1 entries]`, `affects: [04-bayesian-fitting Phase 4 unblock]`, `key-decisions`, `patterns-established`, `duration`, `completed`).
       - Outcome paragraph: final story, final best corr, final chosen Q path.
       - Per-gap evidence summary (one paragraph per 03-05, 03-06, 03-07).
       - Decision: did Phase 3.1 lift SC-2 or definitively document the method limit?
       - Phase 4 readiness note.
       - Files created/modified.
       - Decisions made (story commitment + reasoning).
       - Deviations / issues.

    E) Commit (NEVER skip hooks; let pre-commit run):
       `git add output/circuit_analysis/phase_3_1_evidence.json output/circuit_analysis/wave_b_writeup.md .planning/REQUIREMENTS.md .planning/ROADMAP.md .planning/STATE.md .planning/phases/03-latent-circuit-inference/03-08-SUMMARY.md`
       Commit message:
       ```
       docs(03-08): close Phase 3.1 gap closure (story=<STORY_0|STORY_1>, final_corr=<value>)

       Aggregates evidence from 03-05 (masked-loss), 03-06 (per-context), 03-07 (short-T or skipped) into phase_3_1_evidence.json.
       Updates wave_b_writeup.md with Phase 3.1 Evidence + Closure sections; new story commitment is <STORY_0|STORY_1>.
       Updates REQUIREMENTS.md (CIRC-04 status), ROADMAP.md (Phase 3.1 plans), STATE.md (Phase 4 unblocked).
       ```
       (Per the user's global instructions: do NOT include a Co-Authored-By line.)

    Cluster vs local execution venue: local CPU only — pure JSON parse, markdown editing, git commit.
  </action>
  <verify>
    `git status` shows the six modified files staged in HEAD after commit.
    `grep -c "Phase 3.1" .planning/ROADMAP.md` returns ≥ 1 (new sub-block exists).
    `grep "CIRC-04" .planning/REQUIREMENTS.md` shows updated status.
    03-08-SUMMARY.md frontmatter has all required fields.
    `git log -1 --format=%s` shows the closure commit message.
  </verify>
  <done>
    - All six files updated and committed in a single closure commit.
    - REQUIREMENTS.md CIRC-04 status reflects final outcome.
    - ROADMAP.md Phase 3.1 sub-block lists all four plans with checked boxes.
    - STATE.md current-focus + blockers reflect Phase 3.1 completion.
    - 03-08-SUMMARY.md exists with all required sections.
    - Phase 4 is officially unblocked.
  </done>
</task>

</tasks>

<verification>
1. phase_3_1_evidence.json aggregates ALL upstream evidence from 03-05, 03-06, 03-07 with correct cross-comparable corr values.
2. The story decision rule was applied correctly: STORY_0 if any gap cleared 0.85, STORY_1 if all gap-closures executed (or 03-07 correctly-skipped) without crossing.
3. The user-checkpoint gate is honored: REQUIREMENTS.md / ROADMAP.md updates only happen after explicit "approved" from the user.
4. wave_b_writeup.md is updated in-place (preserves git history of the Story 2 version) with new Phase 3.1 sections and updated story commitment line.
5. Phase 4 is unblocked at the ROADMAP.md and STATE.md level — descriptive role of Q reaffirmed.
6. All numpy scalars cast to Python int/float/bool/str before json.dump (recurring lesson from 03-04, 02-03, 01-03).
</verification>

<success_criteria>
- Phase 3.1 closes with a clearly-committed scientific story backed by aggregated evidence from all three deferred fixes.
- SC-2 status is final (PASSED or DOCUMENTED_LIMIT — never STORY_2 again).
- Phase 4 is unblocked at the planning level.
- The user has reviewed and approved the story re-commit at the checkpoint.
- All changes land in a single, reviewable git commit (no piecemeal landing risk).
</success_criteria>

<output>
After completion, create `.planning/phases/03-latent-circuit-inference/03-08-SUMMARY.md` with:
- Frontmatter (phase, plan: "08", subsystem: planning-closure, gap_closure: true, requires: [03-05, 03-06, 03-07], provides, affects: [04-bayesian-fitting Phase 4 unblock], tech-stack, key-files (modified: ROADMAP, REQUIREMENTS, STATE, wave_b_writeup; created: phase_3_1_evidence.json), key-decisions, patterns-established, duration, completed).
- Outcome paragraph: final story commitment + final best corr + chosen Q path.
- Phase 3.1 evidence chain summary (one paragraph per 03-05, 03-06, 03-07).
- Decision: did Phase 3.1 lift SC-2, or definitively document the method limit?
- Phase 4 readiness note (independent of Q quality, descriptive role).
- Files created/modified.
- Decisions made (story commitment reasoning).
- Deviations / issues.
- Note the cross-plan eval-procedure consistency: cluster_same_seed_as_train was used uniformly across 03-05/06/07 for valid corr comparisons.
</output>
