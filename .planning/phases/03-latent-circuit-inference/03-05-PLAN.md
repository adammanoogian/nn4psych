---
phase: 03-latent-circuit-inference
plan: "05"
type: execute
wave: 5
depends_on: []
gap_closure: true
autonomous: true
files_modified:
  - src/nn4psych/analysis/circuit_inference.py
  - scripts/data_pipeline/08_infer_latent_circuits.py
  - cluster/run_n_latent_sweep_masked.sh
  - data/processed/rnn_behav/circuit_data.npz
  - output/circuit_analysis/n_latent_sweep_masked/
  - output/circuit_analysis/n_latent_sweep_masked/wave_a_masked_selection.json
  - output/circuit_analysis/n_latent_sweep_masked/pareto_curve_masked.json
  - .planning/phases/03-latent-circuit-inference/03-05-SUMMARY.md

must_haves:
  truths:
    - "circuit_data.npz contains a per-trial bool mask `task_active_mask` of shape (n_trials, T) demarcating fixation+stim+decision steps from blank/padding steps, derived from NeuroGym timing dict (not hard-coded)"
    - "fit_latent_circuit_ensemble accepts a `task_active_mask` kwarg (or a sibling fit_latent_circuit_ensemble_masked) that computes NMSE_y over masked timesteps only — verified by running with mask=None reproducing pre-existing per-init nmse_y to numerical equivalence"
    - "A 3-rank cluster sweep at n_latent ∈ {8, 12, 16} with masked loss has executed and per-rank artifacts (best_latent_circuit.pt, validation_results.json, ensemble_diagnostics.json) exist under output/circuit_analysis/n_latent_sweep_masked/n{N}/"
    - "wave_a_masked_selection.json records: chosen_rank (argmax invariant_subspace_corr with same tie-break as Wave A), per-rank invariant_subspace_corr, per-rank nmse_y, the eval procedure used (cluster same-seed-as-train), and a boolean `crossed_85_threshold` flag"
    - "If chosen_rank's masked-corr ≥ 0.85: SC-2 cleared and 03-08 will commit Story 0; if < 0.85, masked corr is still reported with provenance for 03-07/03-08 to consume"
  artifacts:
    - path: "src/nn4psych/analysis/circuit_inference.py"
      provides: "task_active_mask augmentation in collect_circuit_data; masked-loss fit variant"
      contains: "task_active_mask"
    - path: "data/processed/rnn_behav/circuit_data.npz"
      provides: "circuit data with new task_active_mask key alongside u/z/y/labels"
      contains: "task_active_mask"
    - path: "cluster/run_n_latent_sweep_masked.sh"
      provides: "SLURM submitter for masked-loss sweep at ranks {8,12,16} with autopush"
      contains: "OUTPUT_SUBDIR=n_latent_sweep_masked"
    - path: "output/circuit_analysis/n_latent_sweep_masked/wave_a_masked_selection.json"
      provides: "chosen rank + per-rank metrics for downstream plans"
      contains: "chosen_rank"
    - path: "output/circuit_analysis/n_latent_sweep_masked/pareto_curve_masked.json"
      provides: "Pareto table comparable to wave_a pareto_curve.json (same schema)"
      contains: "ranks_attempted"
  key_links:
    - from: "circuit_inference.py::collect_circuit_data"
      to: "env.env.unwrapped.start_t / end_t (NeuroGym TrialEnv timing dict)"
      via: "extract per-period (fixation, stimulus, decision) start/end indices from ngym env, mark mask True over union of active periods"
      pattern: "task_active_mask\\["
    - from: "circuit_inference.py::fit_latent_circuit_ensemble"
      to: "NMSE_y computation"
      via: "broadcast (n_trials, T, 1) mask over residual, sum over masked elements only, divide by sum of mask elements * N"
      pattern: "task_active_mask|mask"
    - from: "scripts/data_pipeline/08_infer_latent_circuits.py"
      to: "fit variant"
      via: "new --masked CLI flag that loads task_active_mask from npz and passes it through"
      pattern: "--masked|masked"
---

<objective>
Implement masked-loss latent circuit fitting (Gap 1, the highest-priority Phase 3.1 deferred fix) and run a 3-rank cluster sweep at n_latent ∈ {8, 12, 16}. Tests the hypothesis that the dominant cause of the SC-2 soft-fail (invariant_subspace_corr=0.783 at n=12, target ≥0.85) is loss budget being burned on the ~45-60 blank/padding timesteps of T=75 rather than the ~15-30 task-active steps.

Purpose: Either lift invariant_subspace_corr ≥ 0.85 — closing SC-2 and pre-empting Plans 03-06 and 03-07 — or definitively rule out "T=75 padding noise" as the cause and move the structural concern from "ran_out_of_fixes" toward "method_limit". Either outcome is consumed by 03-08's writeup re-commit.

Output: Augmented circuit_data.npz with per-trial task_active_mask; masked-loss fit variant in circuit_inference.py; cluster artifacts under output/circuit_analysis/n_latent_sweep_masked/; wave_a_masked_selection.json with chosen rank and threshold flag.
</objective>

<execution_context>
@C:\Users\aman0087\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\aman0087\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-latent-circuit-inference/03-CONTEXT.md
@.planning/phases/03-latent-circuit-inference/03-VERIFICATION.md
@.planning/phases/03-latent-circuit-inference/03-04-SUMMARY.md
@src/nn4psych/analysis/circuit_inference.py
@src/nn4psych/analysis/latent_net.py
@scripts/data_pipeline/08_infer_latent_circuits.py
@cluster/run_n_latent_sweep.sh
@cluster/run_circuit_ensemble.sh
@envs/neurogym_wrapper.py
@output/circuit_analysis/wave_b_writeup.md
@output/circuit_analysis/n_latent_sweep/wave_a_selection.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Augment collect_circuit_data with task_active_mask + add masked-loss fit variant</name>
  <files>
    src/nn4psych/analysis/circuit_inference.py
    data/processed/rnn_behav/circuit_data.npz
  </files>
  <action>
    Edit `src/nn4psych/analysis/circuit_inference.py`:

    A) In `collect_circuit_data` (line ~37): inside the per-trial loop (after `obs, done = env.reset()` at line ~119), extract the NeuroGym task-active timing from `env.env.unwrapped`:
       - NeuroGym TrialEnv stores per-period start/end timesteps in `env.env.unwrapped.start_t` and `env.env.unwrapped.end_t` (dicts keyed by period name, e.g. 'fixation', 'stimulus', 'decision'). Values are in dt units (`env.env.unwrapped.t_ind` increments per step).
       - The active mask should be True for steps where the env is in 'stimulus' or 'decision' periods (NOT 'fixation' — fixation is sensorimotor-only, low information for circuit fitting; this matches the gap hypothesis).
       - Build a per-trial bool array `trial_mask` of length equal to `T_trial`: at each step before `env.step(action)`, query `env.env.unwrapped.in_period('stimulus') or env.env.unwrapped.in_period('decision')` and append. If `in_period` not available, fall back to checking `start_t['stimulus'] <= env.env.unwrapped.t_ind < end_t['decision']`.
       - Wrap in try/except — if the env doesn't expose periods, default mask to all-True with a `warnings.warn(...)` and still produce data (ensures backward-compat).
       - Store `trial_mask` per trial; after truncating to uniform T (line ~196), build `task_active_mask: np.ndarray (n_trials, T) bool` by truncating each trial_mask to T.
       - Add `task_active_mask` to the returned dict.

    B) In `save_circuit_data` (line ~228): include `task_active_mask` in the np.savez call (it's already a generic kwargs path; just confirm the new key flows through).

    C) Add a new sibling function `fit_latent_circuit_ensemble_masked(...)` immediately after `fit_latent_circuit_ensemble` (or add a `task_active_mask: np.ndarray | None = None` kwarg to the existing function — pick whichever introduces less duplication; if you copy, factor the shared body into a private `_fit_one_init` helper). The masked variant differs only in the loss computation:
       - Convert mask to torch tensor once: `mask_t = torch.tensor(task_active_mask, dtype=torch.float32, device=torch_device).unsqueeze(-1)  # (n_trials, T, 1)`
       - In the per-epoch loss inside the existing fitting loop, replace the unmasked NMSE_y:
         `nmse_y = ((y_pred - y_tensor)**2).mean() / (y_tensor**2).mean()`
         with masked NMSE_y:
         `resid_sq = ((y_pred - y_tensor)**2) * mask_t  # zeroed at non-active`
         `denom_sq = (y_tensor**2) * mask_t`
         `nmse_y = resid_sq.sum() / denom_sq.sum().clamp_min(1e-12)`
       - mse_z is also masked the same way (broadcasts over (n_trials, T, output_size)).
       - The validation/post-fit metric reported in the result dict is the masked nmse_y; ALSO compute and report the unmasked nmse_y as `nmse_y_full` for cross-comparability with Wave A.
       - When `task_active_mask is None`, behavior is identical to the original (verify with a unit-style assert in the smoke run below).

    D) Regenerate `data/processed/rnn_behav/circuit_data.npz` LOCALLY via a small one-shot driver (NOT the cluster — local CPU is fine for collection of 1000 trials). Reuse the canonical model at `data/processed/rnn_behav/model_context_dm_dual.pth`. Use `n_trials_per_context=500`, `max_steps=75`, `device='cpu'`. Save `task_active_mask` alongside u/z/y/labels.

       The legacy file `circuit_data.npz.legacy_raw_logits_600` already exists from prior backups. To preserve 03-04's perturbation reproducibility, back up the current canonical file BEFORE overwriting:

       1. FIRST: add `data/processed/rnn_behav/circuit_data.npz.pre_masked_backup` to `.gitignore` (single deterministic line; commit the .gitignore change separately so the backup is never staged). Document the new .gitignore entry in 03-05-SUMMARY.md.
       2. THEN: copy the existing canonical file to the deterministic backup path:
          `cp data/processed/rnn_behav/circuit_data.npz data/processed/rnn_behav/circuit_data.npz.pre_masked_backup`
          (Deterministic name — re-runs of this task will overwrite the prior backup; that's fine since the legacy_raw_logits_600 backup already covers the deeper history.)
       3. THEN: overwrite `circuit_data.npz` with the new mask-augmented version.

       Rationale for deterministic backup name: a `$(date +%s)` suffix would make the backup path non-deterministic, polluting `git status` and risking inclusion in `git add -A` during 03-08 closure. A fixed name is gitignored cleanly.

    E) Verify mask sanity locally: print mean number of True steps per trial — should be in the range [10, 40] (paper hypothesis says ~15-30 active of T=75). Cast counts to Python int before any json.dump (recurring lesson — see 03-04 deviations).

    Conventions: `from __future__ import annotations` already at top; use native type hints (`np.ndarray`, `int | None`); NumPy-style docstring on the new function/kwarg; no mutable default args; absolute imports only; line length 88; cast numpy scalars to Python builtins before json.dump.
  </action>
  <verify>
    Run locally with the project's conda Python:
    `/c/Users/aman0087/AppData/Local/miniforge3/envs/actinf-py-scripts/python.exe -c "import numpy as np; d = np.load('data/processed/rnn_behav/circuit_data.npz'); m = d['task_active_mask']; print('shape', m.shape, 'dtype', m.dtype, 'mean_active_per_trial', float(m.sum(axis=1).mean()), 'min', int(m.sum(axis=1).min()), 'max', int(m.sum(axis=1).max()))"`

    Expected: shape=(1000, 75), dtype=bool, mean_active_per_trial in [10,40], min ≥ 5.

    Then a 1-init smoke fit (n_inits=1, epochs=10, n_latent=12) on a 50-trial slice via:
    `/c/.../python.exe scripts/data_pipeline/08_infer_latent_circuits.py --quick --masked` (or a tiny inline script if the --masked flag isn't exposed yet at this task's boundary).

    Expected: a numeric `nmse_y` (masked) and `nmse_y_full` (unmasked) print, no exception, smoke artifacts under `output/circuit_analysis/smoke_test/`.
  </verify>
  <done>
    - circuit_data.npz contains a `task_active_mask` key of shape (1000, 75), bool, with sane per-trial active counts.
    - The pre-masked circuit_data.npz is backed up under a `.pre_masked_*` filename.
    - Either fit_latent_circuit_ensemble accepts `task_active_mask=` (default None reproducing original behavior) OR a sibling `fit_latent_circuit_ensemble_masked` exists with identical signature plus the kwarg.
    - The smoke fit runs end-to-end and prints both masked and full nmse_y.
    - No new ruff/mypy regressions on the modified file (run `ruff check src/nn4psych/analysis/circuit_inference.py` if available locally).
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire masked-loss CLI + cluster sweep submitter, kick off 3-rank sweep</name>
  <files>
    scripts/data_pipeline/08_infer_latent_circuits.py
    cluster/run_n_latent_sweep_masked.sh
  </files>
  <action>
    A) Edit `scripts/data_pipeline/08_infer_latent_circuits.py`:
       - Add a `--masked` boolean CLI flag (default False).
       - When `--masked` is set: load `task_active_mask` from circuit_data.npz; pass it to the fit (either via the kwarg or by calling the masked sibling function); change the default OUTPUT_SUBDIR (when env var unset) to land in `output/circuit_analysis/n_latent_sweep_masked/n{N}/` so masked artifacts never overwrite Wave A's `n_latent_sweep/n{N}/`.
       - Persist a `masked: true` field in any JSON the script writes (`validation_results.json`, `ensemble_diagnostics.json`) so downstream plan 03-08 can disambiguate masked vs full sweep artifacts.
       - Keep `load_wave_a_chosen_rank()` semantics intact for non-masked path; mirror it with a `load_wave_a_masked_chosen_rank()` that reads `output/circuit_analysis/n_latent_sweep_masked/wave_a_masked_selection.json` (file may not exist yet — return None / fall through).

    B) Create `cluster/run_n_latent_sweep_masked.sh` by copying `cluster/run_n_latent_sweep.sh` and modifying:
       - Default RANKS="8,12,16" (drop n=4: failed assertion in Wave A; not informative here).
       - Pass an extra env var `MASKED=1` through the `--export=...` line into `run_circuit_ensemble.sh`.
       - Set `OUTPUT_SUBDIR=n_latent_sweep_masked/n${N}` (NOT `n_latent_sweep/n${N}`) so masked artifacts are isolated.
       - Job name prefix changes from `circuit_n${N}` to `circuit_masked_n${N}` for clean log filtering.
       - Same autopush wiring (`--dependency=afterany:` then `cluster/99_push_results.slurm`).

    C) Edit `cluster/run_circuit_ensemble.sh` (the inner SLURM script): add a `MASKED=${MASKED:-0}` config line near the other env-var defaults; in the python invocation, if `MASKED=1`, append `--masked` to the `08_infer_latent_circuits.py` invocation (or set a python-side env var that the script checks). Keep the change minimal and isolated; the non-masked Wave A path must remain bit-identical.

    D) After the file edits, smoke-test the script end-to-end LOCALLY (not on cluster) with `--quick --masked`:
       `/c/.../python.exe scripts/data_pipeline/08_infer_latent_circuits.py --quick --masked`
       Verify it lands artifacts under `output/circuit_analysis/smoke_test/` (the smoke_test path redirect from 03-04 must still apply when --quick is also set; this is a regression check).

    E) Commit and push the updated `circuit_data.npz` (with `task_active_mask`) BEFORE submitting the cluster sweep, and confirm `git pull` on the cluster shows the new file. The cluster's `run_circuit_ensemble.sh` has a flock guard that, if the npz is missing, re-collects from scratch (full RNN re-train at EPOCHS=150) — without `task_active_mask` — and silently produces results labeled "masked" but actually testing nothing.

       Steps:
       1. `git add data/processed/rnn_behav/circuit_data.npz && git commit -m "data(03-05): add task_active_mask to circuit_data.npz"`
       2. `git push origin main`
       3. On cluster (or via the autopush bookkeeping you already use), confirm `git log -1 --stat data/processed/rnn_behav/circuit_data.npz` shows the new commit hash.
       4. ADDITIONALLY, add a cluster-side assertion in `cluster/run_circuit_ensemble.sh` (or in the heredoc Python block it dispatches): if `MASKED=1` and the loaded npz does NOT contain key `task_active_mask`, abort with a clear error (`raise KeyError("MASKED=1 set but circuit_data.npz lacks 'task_active_mask' key — cluster checkout is stale; pull latest main")`) rather than silently falling back to full-T fitting. Place the assertion immediately after the `np.load(...)` call inside the python block and ONLY trigger when MASKED=1 (so the Wave A non-masked path is unaffected).
       5. Then submit the cluster sweep:
          `bash cluster/run_n_latent_sweep_masked.sh --ranks 8,12,16`
          This submits 3 fitting jobs + 1 autopush job. Capture job IDs to `cluster/logs/n_latent_sweep_masked_jobs.txt`. EVAL CONSISTENCY: the masked sweep uses the same cluster-style same-seed-as-train metric reporting as Wave A (both compute corr immediately after training on the same noise realization) — do not switch to fresh-seed eval here (it would invalidate cross-comparison with Wave A's 0.783).

    F) Cluster execution venue: GPU on Monash M3 via SLURM. Local execution venue: only `--quick` smoke. NEVER run a full 100-init fit locally (16GB RAM limit + 3-4 GPU-hours per rank).

    Cast all numpy scalars (per-rank corr, nmse_y, durations) to Python int/float before json.dump in any aggregator that runs (recurring lesson from 03-04, 02-03, 01-03).
  </action>
  <verify>
    `bash cluster/run_n_latent_sweep_masked.sh --ranks 8,12,16` returns 3 fitting JOB_IDs + 1 push JOB_ID; `cat cluster/logs/n_latent_sweep_masked_jobs.txt` shows 4 lines.

    `--quick --masked` smoke locally produces `output/circuit_analysis/smoke_test/validation_results.json` containing `"masked": true`.

    After cluster jobs complete (autopush will commit results back to repo), pull and check that `output/circuit_analysis/n_latent_sweep_masked/n8/best_latent_circuit.pt`, `n12/best_latent_circuit.pt`, `n16/best_latent_circuit.pt` all exist.
  </verify>
  <done>
    - `--masked` CLI flag exists in 08_infer_latent_circuits.py.
    - `cluster/run_n_latent_sweep_masked.sh` exists, executable, default ranks 8/12/16, autopush wired.
    - `cluster/run_circuit_ensemble.sh` honors `MASKED=1` env var and propagates `--masked` to the python script.
    - Cluster sweep submitted; 3 fit jobs + 1 push job recorded.
    - Local `--quick --masked` smoke produces `"masked": true` in the artifact.
    - Wave A's `output/circuit_analysis/n_latent_sweep/` is untouched (no path collision).
  </done>
</task>

<task type="auto">
  <name>Task 3: Aggregate masked-sweep results, write wave_a_masked_selection.json with crossed_85_threshold flag</name>
  <files>
    scripts/analysis/aggregate_n_latent_sweep_masked.py
    output/circuit_analysis/n_latent_sweep_masked/pareto_curve_masked.json
    output/circuit_analysis/n_latent_sweep_masked/wave_a_masked_selection.json
    .planning/phases/03-latent-circuit-inference/03-05-SUMMARY.md
  </files>
  <action>
    Wait for the cluster autopush to land masked artifacts in the repo (squeue / git pull until n_latent_sweep_masked/n{8,12,16}/ subdirs are present).

    A) Create `scripts/analysis/aggregate_n_latent_sweep_masked.py` by copy-adapting `scripts/analysis/aggregate_n_latent_sweep.py`:
       - Read per-rank `validation_results.json` from `output/circuit_analysis/n_latent_sweep_masked/n{N}/`.
       - For each rank, record: n_latent, masked_nmse_y, nmse_y_full, invariant_subspace_corr, trial_avg_r2_full, wall_min, n_inits.
       - Apply the SAME selection rule as Wave A: chosen_rank = argmax(invariant_subspace_corr) with tie-break (lower-rank wins) — preserves cross-sweep comparability.
       - Write `output/circuit_analysis/n_latent_sweep_masked/pareto_curve_masked.json` with the same schema as Wave A's `pareto_curve.json` (so downstream tooling that ingests both can union them) plus a top-level `masked: true` field and a `comparable_full_pareto: "output/circuit_analysis/n_latent_sweep/pareto_curve.json"` cross-reference.
       - Write `output/circuit_analysis/n_latent_sweep_masked/wave_a_masked_selection.json`:
         ```
         {
           "chosen_rank": <int>,
           "metrics": {"invariant_subspace_corr": <float>, "masked_nmse_y": <float>, "nmse_y_full": <float>, "trial_avg_r2_full": <float>},
           "all_ranks": [{"n": 8, "corr": ...}, {"n": 12, "corr": ...}, {"n": 16, "corr": ...}],
           "crossed_85_threshold": <bool>,         # corr >= 0.85 at chosen_rank
           "delta_vs_wave_a": <float>,             # corr - 0.7833 (Wave A n=12 baseline)
           "eval_procedure": "cluster_same_seed_as_train",
           "masked": true,
           "comparable_full_selection": "output/circuit_analysis/n_latent_sweep/wave_a_selection.json"
         }
         ```
       - Cast every numpy scalar to Python int/float before json.dump.

    B) Run the aggregator:
       `/c/.../python.exe scripts/analysis/aggregate_n_latent_sweep_masked.py`

    C) Optionally render `pareto_curve_masked.png` (matplotlib with `matplotlib.use('Agg')` BEFORE pyplot import — never `plt.show()`); not strictly required but helpful for 03-08's writeup. Two-curve overlay (full vs masked corr-vs-rank) is the most useful diagnostic.

    D) Write `.planning/phases/03-latent-circuit-inference/03-05-SUMMARY.md` per the standard SUMMARY template. Include:
       - Per-rank table (n, masked_corr, nmse_y_full, masked_nmse_y, wall_min).
       - Comparison row vs Wave A (delta corr at n=12 specifically).
       - Pass/fail status: did chosen_rank cross 0.85?
       - Decision flag for 03-07: if masked corr ≥ 0.85, then 03-07 (shorter-T regen) is no-op (skip); if masked corr < 0.85 and Pareto is FLAT (e.g., spread < 0.05 across ranks), tilt the Phase 3.1 story toward "method_limit"; if Pareto still has spread, 03-07 should be run.
       - Files produced.
       - Cast all numpy values to Python builtins before json.dump.
  </action>
  <verify>
    `cat output/circuit_analysis/n_latent_sweep_masked/wave_a_masked_selection.json` shows valid JSON with all five top-level fields, chosen_rank in {8,12,16}, crossed_85_threshold is a bool.

    `python -c "import json; d = json.load(open('output/circuit_analysis/n_latent_sweep_masked/pareto_curve_masked.json')); assert d['masked'] is True; assert len(d['rows']) == 3; print('OK')"`

    SUMMARY.md committed with task table.
  </verify>
  <done>
    - `wave_a_masked_selection.json` exists with all required fields, all numbers are Python types.
    - `pareto_curve_masked.json` exists with 3 rows and `masked: true`.
    - 03-05-SUMMARY.md exists with per-rank table, comparison vs Wave A, and decision-flag for 03-07.
    - All artifacts committed; cluster autopush + local commit complete.
  </done>
</task>

</tasks>

<verification>
1. `task_active_mask` exists in circuit_data.npz with shape (1000, 75), bool, sane per-trial active counts (10–40 mean).
2. `fit_latent_circuit_ensemble` (or sibling) accepts a mask kwarg; mask=None reproduces full-loss behavior (sanity-check by re-fitting one init with mask=None and confirming nmse_y matches a Wave A reference within 1% on the same data).
3. Cluster sweep produced 3 per-rank artifact directories under `output/circuit_analysis/n_latent_sweep_masked/`.
4. `wave_a_masked_selection.json` exists, parseable, contains `crossed_85_threshold` as a bool.
5. Eval procedure documented as `cluster_same_seed_as_train` — consistent with Wave A's eval procedure for valid cross-comparison.
6. `output/circuit_analysis/n_latent_sweep/` (Wave A) is untouched.
</verification>

<success_criteria>
- Either: chosen_rank's masked invariant_subspace_corr ≥ 0.85 → SC-2 cleared; 03-08 will commit Story 0; 03-06 and 03-07 may be no-ops.
- Or: chosen_rank's masked corr < 0.85, but reported with provenance and delta_vs_wave_a logged. Continue to 03-06 (diagnostic) and 03-07 (if Pareto spread still suggests structural rather than method limit).
- In all cases: the masked-loss path is now permanently in circuit_inference.py and 08_infer_latent_circuits.py, available for v2 / future work; Wave A artifacts and Phase 4 are uncoupled from the outcome.
</success_criteria>

<output>
After completion, create `.planning/phases/03-latent-circuit-inference/03-05-SUMMARY.md` with:
- Frontmatter (`phase`, `plan: "05"`, `subsystem: analysis`, `gap_closure: true`, `tags`, `requires`, `provides`, `affects`, `tech-stack`, `key-files`, `key-decisions`, `patterns-established`, `duration`, `completed`).
- Outcome paragraph: did masked corr cross 0.85? delta vs Wave A?
- Per-rank Pareto table (masked vs full).
- Eval-procedure note (cluster same-seed-as-train).
- Decision flag for 03-07 (skip / run).
- Files created/modified.
- Decisions made (e.g., "fixation excluded from mask because fixation is sensorimotor-only").
- Deviations (if any).
- Issues encountered.
</output>
