---
phase: 01-infrastructure-and-organization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/nn4psych/bayesian/__init__.py
  - src/nn4psych/bayesian/numpyro_models.py
  - src/nn4psych/bayesian/model_comparison.py
  - src/nn4psych/bayesian/visualization.py
  - scripts/fitting/fit_bayesian_numpyro.py
  - scripts/fitting/fit_nassar_numpyro.py
  - scripts/fitting/batch_fit_bayesian.py
autonomous: true

must_haves:
  truths:
    - "`from nn4psych.bayesian import run_mcmc` succeeds after pip install -e ."
    - "`from nn4psych.bayesian.numpyro_models import compute_normative_model` resolves"
    - "`from nn4psych.bayesian.visualization import plot_model_fit_comprehensive` resolves"
    - "model_comparison.py no longer imports from bayesian.pyem_models"
    - "JAX CPU-only is enforced when bayesian subpackage is imported"
  artifacts:
    - path: "src/nn4psych/bayesian/__init__.py"
      provides: "Package init with JAX CPU enforcement and NumPyro-only exports"
      contains: "os.environ.setdefault"
    - path: "src/nn4psych/bayesian/numpyro_models.py"
      provides: "NumPyro MCMC model (moved from bayesian/)"
      contains: "def compute_normative_model"
    - path: "src/nn4psych/bayesian/model_comparison.py"
      provides: "BIC/AIC comparison (PyEM import removed)"
      contains: "def calculate_bic"
    - path: "src/nn4psych/bayesian/visualization.py"
      provides: "Plotting functions (moved from bayesian/)"
      contains: "def plot_model_fit_comprehensive"
  key_links:
    - from: "src/nn4psych/bayesian/__init__.py"
      to: "src/nn4psych/bayesian/numpyro_models.py"
      via: "from nn4psych.bayesian.numpyro_models import run_mcmc"
      pattern: "from nn4psych\\.bayesian\\.numpyro_models import"
    - from: "src/nn4psych/bayesian/__init__.py"
      to: "os.environ"
      via: "JAX_PLATFORM_NAME setdefault before any JAX import"
      pattern: 'os\.environ\.setdefault\("JAX_PLATFORM_NAME"'
---

<objective>
Move the bayesian/ package from project root into src/nn4psych/bayesian/ so that `import nn4psych.bayesian` works after `pip install -e .`. Create a new __init__.py that exports only NumPyro functions (no PyMC/PyEM), enforces JAX CPU-only via env vars, and update all script imports from `from bayesian.X` to `from nn4psych.bayesian.X`. Remove the pyem_models import from model_comparison.py (the `compare_contexts` and `cross_validate_k_fold` functions that call `fit()` will be left as stubs or removed since PyEM is being archived in Plan 01-02).

Purpose: After this plan, `import nn4psych.bayesian` works from any Python environment with `pip install -e .`, and the package is structured for installability.
Output: Four files in src/nn4psych/bayesian/, updated script imports.
</objective>

<execution_context>
@C:\Users\aman0087\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\aman0087\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-infrastructure-and-organization/01-RESEARCH.md
@bayesian/__init__.py
@bayesian/numpyro_models.py
@bayesian/model_comparison.py
@bayesian/visualization.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move bayesian/ files to src/nn4psych/bayesian/ and create new __init__.py</name>
  <files>
    src/nn4psych/bayesian/__init__.py
    src/nn4psych/bayesian/numpyro_models.py
    src/nn4psych/bayesian/model_comparison.py
    src/nn4psych/bayesian/visualization.py
  </files>
  <action>
1. Create `src/nn4psych/bayesian/` directory.

2. Copy `bayesian/numpyro_models.py` to `src/nn4psych/bayesian/numpyro_models.py` — no content changes needed (bug fix happens in Plan 01-03).

3. Copy `bayesian/visualization.py` to `src/nn4psych/bayesian/visualization.py` — no content changes needed (pure matplotlib/seaborn, no bayesian imports).

4. Copy `bayesian/model_comparison.py` to `src/nn4psych/bayesian/model_comparison.py`. Then edit it:
   - Remove line 14: `from bayesian.pyem_models import fit`
   - In `compare_contexts()` (line 92-176): The function calls `fit(...)` from pyem_models. Since PyEM is being archived, remove the `fit()` calls and replace `compare_contexts` with a version that accepts pre-computed negative log-likelihoods instead of calling `fit()` internally. Keep the signature but change `fitted_params_cp`/`fitted_params_ob` to `negll_cp`/`negll_ob` (float). Remove the internal `fit()` calls at lines 133-149. Keep `calculate_bic`, `calculate_aic`, `calculate_aicc`, `likelihood_ratio_test`, `model_weights_aic`, `print_comparison_summary` unchanged (they are pure NumPy).
   - In `cross_validate_k_fold()` (line 179-271): This function calls `fit()` from pyem_models in minimize() and for test evaluation. Since PyEM is being archived, remove this function entirely — it was coupled to the PyEM optimization loop. NumPyro cross-validation will be implemented differently in Phase 4.

5. Create `src/nn4psych/bayesian/__init__.py` with this exact content:

```python
"""
Bayesian subpackage for nn4psych.

Provides NumPyro-based MCMC inference for normative models.
Enforces JAX CPU-only operation for PyTorch coexistence.
"""
import os

# Must be set before any JAX import — enforces CPU backend
# and prevents pre-allocation that conflicts with PyTorch GPU memory.
os.environ.setdefault("JAX_PLATFORM_NAME", "cpu")
os.environ.setdefault("XLA_PYTHON_CLIENT_PREALLOCATE", "false")

from nn4psych.bayesian.numpyro_models import (
    run_mcmc,
    summarize_posterior,
    posterior_predictive,
    compute_waic,
    get_map_estimate,
    compute_normative_model,
)

__all__ = [
    "run_mcmc",
    "summarize_posterior",
    "posterior_predictive",
    "compute_waic",
    "get_map_estimate",
    "compute_normative_model",
]
```

Key points:
- `os.environ.setdefault` (not direct assignment) allows users to override.
- `JAX_PLATFORM_NAME` and `XLA_PYTHON_CLIENT_PREALLOCATE` MUST be set BEFORE the `from nn4psych.bayesian.numpyro_models import ...` line, because that import triggers `import jax` at module level.
- No PyMC or PyEM imports. No `BayesianModel`. No `fit_bayesian_model`.

6. Do NOT delete the original `bayesian/` directory yet — Plan 01-02 will handle archiving bayesian_models.py and pyem_models.py, and the old bayesian/ can be cleaned up after both plans complete.
  </action>
  <verify>
Run: `python -c "from nn4psych.bayesian import run_mcmc, compute_normative_model; print('OK')"`
Expected: prints "OK" without ImportError.

Run: `python -c "from nn4psych.bayesian.model_comparison import calculate_bic; print('OK')"`
Expected: prints "OK" — no pyem_models import error.

Run: `python -c "from nn4psych.bayesian.visualization import plot_model_fit_comprehensive; print('OK')"` (if this function exists) or check that the module imports without error.
  </verify>
  <done>
- src/nn4psych/bayesian/ exists with __init__.py, numpyro_models.py, model_comparison.py, visualization.py
- `from nn4psych.bayesian import run_mcmc` works
- model_comparison.py has no import of pyem_models
- __init__.py sets JAX_PLATFORM_NAME before any JAX import
  </done>
</task>

<task type="auto">
  <name>Task 2: Update script imports from `bayesian.X` to `nn4psych.bayesian.X`</name>
  <files>
    scripts/fitting/fit_bayesian_numpyro.py
    scripts/fitting/fit_nassar_numpyro.py
    scripts/fitting/batch_fit_bayesian.py
  </files>
  <action>
Update imports in scripts that reference the NumPyro-based bayesian modules. Only update scripts that will continue to be used (NumPyro scripts). PyMC and PyEM scripts (fit_bayesian_pymc.py, fit_bayesian_pyem.py) will be archived in Plan 01-02.

1. `scripts/fitting/fit_bayesian_numpyro.py` line 33:
   Change: `from bayesian.numpyro_models import (`
   To: `from nn4psych.bayesian.numpyro_models import (`

2. `scripts/fitting/fit_nassar_numpyro.py` line 32:
   Change: `from bayesian.numpyro_models import run_mcmc, summarize_posterior, get_map_estimate`
   To: `from nn4psych.bayesian.numpyro_models import run_mcmc, summarize_posterior, get_map_estimate`

3. `scripts/fitting/batch_fit_bayesian.py`:
   - Line 29: Change `from bayesian import fit_bayesian_model, norm2alpha, norm2beta` — This imports PyEM functions. Since PyEM is being archived, remove this line entirely.
   - Line 30: Change `from bayesian.model_comparison import calculate_bic, calculate_aic` to `from nn4psych.bayesian.model_comparison import calculate_bic, calculate_aic`
   - Line 31: Change `from bayesian.visualization import (` to `from nn4psych.bayesian.visualization import (`
   - Note: `batch_fit_bayesian.py` uses `fit_bayesian_model` (PyEM) extensively. After removing the PyEM import, the script will have broken references. Add a comment at the top: `# TODO: This script needs updating for NumPyro — PyEM fit_bayesian_model was removed`. Do NOT rewrite the script logic; that's Phase 4 work.

Do NOT update:
- `scripts/fitting/fit_bayesian_pymc.py` — will be archived in Plan 01-02
- `scripts/fitting/fit_bayesian_pyem.py` — will be archived in Plan 01-02
- `bayesian/__init__.py` — old package, will be cleaned up later
- `tests/test_imports.py` — these test the old import paths; tests will be updated when old bayesian/ is removed
  </action>
  <verify>
Run: `grep -rn "from bayesian" scripts/fitting/fit_bayesian_numpyro.py scripts/fitting/fit_nassar_numpyro.py`
Expected: No matches (all converted to nn4psych.bayesian).

Run: `grep -n "from nn4psych.bayesian" scripts/fitting/fit_bayesian_numpyro.py scripts/fitting/fit_nassar_numpyro.py scripts/fitting/batch_fit_bayesian.py`
Expected: Shows the updated import lines.
  </verify>
  <done>
- fit_bayesian_numpyro.py imports from nn4psych.bayesian.numpyro_models
- fit_nassar_numpyro.py imports from nn4psych.bayesian.numpyro_models
- batch_fit_bayesian.py imports from nn4psych.bayesian.model_comparison and nn4psych.bayesian.visualization
- No script in scripts/fitting/ imports `from bayesian.numpyro_models` (old path)
  </done>
</task>

</tasks>

<verification>
1. `python -c "from nn4psych.bayesian import run_mcmc; print('import OK')"` prints "import OK"
2. `python -c "import os; from nn4psych.bayesian import run_mcmc; print(os.environ.get('JAX_PLATFORM_NAME'))"` prints "cpu"
3. `python -c "from nn4psych.bayesian.model_comparison import calculate_bic, calculate_aic; print('OK')"` prints "OK"
4. `grep -rn "from bayesian\." scripts/fitting/fit_bayesian_numpyro.py scripts/fitting/fit_nassar_numpyro.py` returns empty (no old imports)
</verification>

<success_criteria>
- `import nn4psych.bayesian` works from clean `pip install -e .`
- JAX CPU enforcement is active (env var set before JAX import)
- model_comparison.py has no PyEM dependency
- Active NumPyro scripts use `nn4psych.bayesian.*` import paths
</success_criteria>

<output>
After completion, create `.planning/phases/01-infrastructure-and-organization/01-01-SUMMARY.md`
</output>
