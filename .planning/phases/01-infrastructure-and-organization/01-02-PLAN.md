---
phase: 01-infrastructure-and-organization
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - archive/bayesian_pymc/bayesian_models.py
  - archive/bayesian_pymc/pyem_models.py
  - archive/bayesian_pymc/README.md
autonomous: true

must_haves:
  truths:
    - "`pip install -e '.[bayesian]'` installs jax, jaxlib, numpyro, arviz — and does NOT install pymc or pytensor"
    - "requires-python is >=3.11 in pyproject.toml"
    - "bayesian_models.py and pyem_models.py are in archive/bayesian_pymc/, not in src/"
    - "dev tools (pytest, ruff, mypy) have version pins matching project_utils conventions"
  artifacts:
    - path: "pyproject.toml"
      provides: "Updated dependency declarations"
      contains: "numpyro>=0.13.0"
    - path: "archive/bayesian_pymc/bayesian_models.py"
      provides: "Archived PyMC implementation"
    - path: "archive/bayesian_pymc/pyem_models.py"
      provides: "Archived PyEM implementation"
  key_links:
    - from: "pyproject.toml"
      to: "[project.optional-dependencies].bayesian"
      via: "extras declaration"
      pattern: "numpyro>=0\\.13\\.0"
---

<objective>
Update pyproject.toml to declare the correct dependency stack (NumPyro/JAX/ArviZ, no PyMC/PyTensor), bump Python to >=3.11, update dev tool version pins to match project_utils conventions, and archive the PyMC/PyEM model files to archive/bayesian_pymc/.

Purpose: After this plan, `pip install -e ".[bayesian]"` installs the correct Bayesian stack, and deprecated model implementations are archived but preserved in git history.
Output: Updated pyproject.toml, archived PyMC/PyEM files.
</objective>

<execution_context>
@C:\Users\aman0087\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\aman0087\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-infrastructure-and-organization/01-RESEARCH.md
@pyproject.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update pyproject.toml dependencies, Python version, and tool config</name>
  <files>pyproject.toml</files>
  <action>
Edit pyproject.toml with these specific changes:

1. **Python version** (line 10):
   Change: `requires-python = ">=3.8"`
   To: `requires-python = ">=3.11"`

2. **Classifiers** (lines 20-24):
   Remove Python 3.8, 3.9, 3.10 classifiers. Keep 3.11, add 3.12:
   ```
   "Programming Language :: Python :: 3",
   "Programming Language :: Python :: 3.11",
   "Programming Language :: Python :: 3.12",
   ```

3. **Optional dependencies — bayesian extra** (lines 46-50):
   Replace the entire `bayesian` extra:
   ```toml
   bayesian = [
       "jax>=0.4.0",
       "jaxlib>=0.4.0",
       "numpyro>=0.13.0",
       "arviz>=0.17.0",
   ]
   ```
   This removes `pymc>=4.0.0` and `pytensor>=2.0.0`, adds `jax`, `jaxlib`, `numpyro`.

4. **Remove standalone jax extra** (lines 51-55):
   Delete the entire `[jax]` extra section:
   ```toml
   jax = [
       "jax>=0.4.0",
       "jaxlib>=0.4.0",
       "optax>=0.1.0",
   ]
   ```
   JAX is now only available through `[bayesian]`. There is no standalone JAX use case in this project.

5. **Dev tools version pins** (lines 39-45):
   Update to match project_utils conventions:
   ```toml
   dev = [
       "pytest>=8.0",
       "pytest-cov>=5.0",
       "ruff>=0.4",
       "mypy>=1.10",
       "pre-commit>=2.13",
   ]
   ```

6. **Ruff target-version** (line 70):
   Change: `target-version = "py310"`
   To: `target-version = "py311"`

7. **Mypy python_version** (line 89):
   Change: `python_version = "3.10"`
   To: `python_version = "3.11"`

8. **Add ruff format section** (after `[tool.ruff.lint.pydocstyle]`):
   ```toml
   [tool.ruff.format]
   quote-style = "double"
   indent-style = "space"
   ```

9. **Add ruff per-file-ignores** (after the format section):
   ```toml
   [tool.ruff.lint.per-file-ignores]
   "tests/*" = ["S101", "D"]
   "scripts/*" = ["D", "T201"]
   ```

10. **Add pytest filterwarnings** (inside `[tool.pytest.ini_options]`):
    ```toml
    filterwarnings = ["ignore::DeprecationWarning"]
    ```

11. **Build system versions** (line 2):
    Change: `requires = ["setuptools>=45", "wheel", "setuptools-scm>=6.2"]`
    To: `requires = ["setuptools>=68.0", "wheel", "setuptools-scm>=8.0"]`

Do NOT change: project name, version, description, core dependencies (numpy, scipy, matplotlib, torch, gymnasium, pyyaml, tqdm), URLs, or setuptools.packages.find config.
  </action>
  <verify>
Run: `python -c "import tomllib; d=tomllib.load(open('pyproject.toml','rb')); print(d['project']['requires-python']); print(d['project']['optional-dependencies']['bayesian']); print('jax' not in d['project']['optional-dependencies'])"`
Expected output:
```
>=3.11
['jax>=0.4.0', 'jaxlib>=0.4.0', 'numpyro>=0.13.0', 'arviz>=0.17.0']
True
```
(The last True confirms standalone `[jax]` extra is gone.)

Run: `grep -c "pymc\|pytensor" pyproject.toml`
Expected: 0 (no references to PyMC or PyTensor).
  </verify>
  <done>
- pyproject.toml has requires-python >= 3.11
- [bayesian] extra contains jax, jaxlib, numpyro, arviz — no pymc, no pytensor
- Standalone [jax] extra is removed
- Dev tool pins match project_utils conventions (pytest>=8.0, ruff>=0.4, mypy>=1.10)
- ruff target-version is py311, mypy python_version is 3.11
  </done>
</task>

<task type="auto">
  <name>Task 2: Archive PyMC and PyEM model files</name>
  <files>
    archive/bayesian_pymc/bayesian_models.py
    archive/bayesian_pymc/pyem_models.py
    archive/bayesian_pymc/README.md
  </files>
  <action>
1. Create `archive/bayesian_pymc/` directory (archive/ already exists with legacy_analysis/, legacy_scripts/, etc.).

2. Copy `bayesian/bayesian_models.py` to `archive/bayesian_pymc/bayesian_models.py` — no content changes. This is the PyMC implementation that imports `pymc`, `pytensor`, etc.

3. Copy `bayesian/pyem_models.py` to `archive/bayesian_pymc/pyem_models.py` — no content changes. This is the PyEM implementation.

4. Create `archive/bayesian_pymc/README.md` with brief explanation:

```
# Archived Bayesian Models (PyMC / PyEM)

These files were the original Bayesian model implementations using PyMC and a custom PyEM framework.
They have been superseded by the NumPyro implementation at `src/nn4psych/bayesian/numpyro_models.py`.

## Files

- `bayesian_models.py` — PyMC-based MCMC model (requires pymc, pytensor)
- `pyem_models.py` — Custom PyEM-based MLE/MAP estimation (pure numpy/scipy)

## Why Archived

- NumPyro/JAX is faster and composable with the rest of the JAX-based analysis pipeline
- PyMC and PyTensor add heavy dependencies that conflict with JAX
- The PyEM approach provides only point estimates; NumPyro provides full posteriors

## Restoring

These files are preserved in git history. To restore:
```
git show HEAD:bayesian/bayesian_models.py > bayesian_models.py
git show HEAD:bayesian/pyem_models.py > pyem_models.py
```
```

5. Also copy `scripts/fitting/fit_bayesian_pymc.py` to `archive/bayesian_pymc/fit_bayesian_pymc.py` and `scripts/fitting/fit_bayesian_pyem.py` to `archive/bayesian_pymc/fit_bayesian_pyem.py` — these scripts depend on the archived implementations and should be archived alongside them.

6. Do NOT delete the original bayesian/ directory contents yet. The old `bayesian/` directory will be cleaned up after both Plan 01-01 and 01-02 complete. Leave a note in the summary about the cleanup needed.
  </action>
  <verify>
Run: `ls archive/bayesian_pymc/`
Expected: bayesian_models.py, pyem_models.py, README.md, fit_bayesian_pymc.py, fit_bayesian_pyem.py

Run: `head -5 archive/bayesian_pymc/bayesian_models.py`
Expected: Shows the PyMC model file header.
  </verify>
  <done>
- archive/bayesian_pymc/ contains bayesian_models.py, pyem_models.py, README.md
- PyMC/PyEM fitting scripts also archived
- Original files still in bayesian/ (cleanup deferred to avoid Plan 01-01 conflict)
  </done>
</task>

</tasks>

<verification>
1. `python -c "import tomllib; d=tomllib.load(open('pyproject.toml','rb')); b=d['project']['optional-dependencies']['bayesian']; assert 'numpyro>=0.13.0' in b; assert all('pymc' not in x for x in b); print('PASS')"` prints PASS
2. `test -d archive/bayesian_pymc && echo "archive exists"` prints "archive exists"
3. `grep "pymc\|pytensor" pyproject.toml | wc -l` returns 0
4. `python -c "import tomllib; d=tomllib.load(open('pyproject.toml','rb')); assert d['project']['requires-python'] == '>=3.11'; print('PASS')"` prints PASS
</verification>

<success_criteria>
- `pip install -e ".[bayesian]"` installs JAX, NumPyro, ArviZ — no PyMC, no PyTensor
- Python requirement is >=3.11
- PyMC and PyEM files are archived in archive/bayesian_pymc/
- Dev tool versions match project_utils conventions
</success_criteria>

<output>
After completion, create `.planning/phases/01-infrastructure-and-organization/01-02-SUMMARY.md`
</output>
